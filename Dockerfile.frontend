# Frontend: Vue 3 + Vite 
FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm ci

COPY . .
# Vite dev-сервер слушает на 0.0.0.0 для доступа с хоста
ENV VITE_DEV_SERVER_HOST=0.0.0.0
EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
